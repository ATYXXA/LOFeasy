{
  "name": "lofeasy",
  "version": "0.1.0",
  "private": true,
  "description": "A-share listed LOF premium ranking API",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test",
    "export:static": "npm run build && node dist/src/export-static.js",
    "start": "node dist/src/index.js",
    "dev": "npm run build && node dist/src/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=22"
  }
}
